# Secrets — the build context goes straight into a layer, never let these in.
config.yml
.env
*.seed

# Host build artifacts: the linux binary is rebuilt inside the image.
build
build.sh
deployment
dexpay
main
shopble
model

# Not part of the Go build, and together the largest thing in this repo. The backend
# does not embed the frontend (no go:embed anywhere), and the contract is Rust.
project/shopble/web/node_modules
project/shopble/web/dist
contracts/target
test_snapshots
.stellar

.git
.gitignore
.claude
.codex
.agents
.vscode

*.md
docker-compose.yml
*.session.sql
*.bak

out/*
